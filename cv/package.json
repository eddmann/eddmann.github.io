{
  "private": true,
  "scripts": {
    "serve": "resume serve --theme .  --resume cv.json",
    "build:pdf": "resume export --theme . --resume cv.json cv.pdf",
    "build:html": "resume export --theme . --resume cv.json cv.html",
    "build:txt": "node text.js cv.json cv.txt"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "html-minifier": "^4.0.0",
    "marked": "^11.1.1"
  },
  "devDependencies": {
    "resume-cli": "^3.0.8"
  }
}
